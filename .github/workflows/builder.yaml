name: Builder

env:
  BUILD_ARGS: ""
  MONITORED_FILES: "hacollector"

on:
  release:
    types: [published]
  # (선택 사항) 테스트를 위해 main 브랜치 푸시 시에도 동작하게 하려면 아래 주석 해제
  # push:
  #   branches:
  #     - main

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build ${{ matrix.arch }} ${{ matrix.addon }}
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        addon: ["hacollector"]
        arch: [aarch64, amd64]

    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3.0.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 릴리즈 태그(v1.0.0)를 읽어서 파일 내부 버전(1.0.0)과 동기화
      - name: Sync version with Release Tag
        if: github.event_name == 'release'
        run: |
          # 태그 이름 가져오기 (예: v1.3.25)
          TAG_NAME=${{ github.event.release.tag_name }}
          # 앞의 'v' 제거 (예: 1.3.25)
          VERSION=${TAG_NAME#v}
          echo "Release detected. Syncing version to $VERSION"

          # 1. config.yaml 버전 수정 (hacollector/config.yaml)
          sed -i "s/^version: \".*\"/version: \"$VERSION\"/" hacollector/config.yaml
          
          # 2. consts.py 버전 수정 (hacollector/consts.py)
          # 공백 유연하게 대응 (SW_VERSION = "..." 패턴 찾아서 교체)
          sed -i "s/^SW_VERSION *= *\".*\"/SW_VERSION          = \"$VERSION\"/" hacollector/consts.py
          
          # 변경 확인
          grep "version:" hacollector/config.yaml
          grep "SW_VERSION" hacollector/consts.py

      - name: Build ${{ matrix.addon }} add-on
        uses: home-assistant/builder@master
        with:
          args: |
            ${{ env.BUILD_ARGS }} \
            --${{ matrix.arch }} \
            --target ${{ matrix.addon }} \
            --image "${{ matrix.addon }}/{arch}" \
            --docker-hub "ghcr.io/${{ github.repository_owner }}" \
            --addon
